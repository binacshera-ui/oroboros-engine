# WASM Configuration for GLITCH WARS
# Enables getrandom JS backend for browser builds

[target.wasm32-unknown-unknown]
rustflags = [
    "--cfg", "getrandom_backend=\"wasm_js\"",
    "--cfg=web_sys_unstable_apis"
]
